import React, { useState, useReducer, useCallback } from 'react';
import {
    ScrollView,
    View,
    KeyboardAvoidingView,
    StyleSheet,
    Button
} from 'react-native';

const AuthScreen = props => {
    return (
        <View><Text>authentification screen</Text>
        </View>
    )
}

export default AuthScreen;